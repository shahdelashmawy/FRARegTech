[build]
builder = "nixpacks"
buildCommand = "npm install --legacy-peer-deps && npm run build && npm install -g serve"

[deploy]
startCommand = "serve -s build -l $PORT"
healthcheckPath = "/"
healthcheckTimeout = 30
